[Unit]
Description=Redirect HTTP requests
After=network.target

[Service]
Type=forking
ExecStart="{{ GOBIN }}/redirect {{ item }}"
ExecReload=/bin/kill -HUP $MAINPID
KillMode=process
Restart=on-failure
RestartSec=42s


[Install]
WantedBy=multi-user.target